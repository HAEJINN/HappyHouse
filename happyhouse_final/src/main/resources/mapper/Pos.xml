<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.happyhouse.model.mapper.PosMapper">
	
	<select id="convenlist" resultType="PosDto">
		select * from conven;
	</select>
	
	<select id="cctvlist" resultType="PosDto">
		select * from cctv;
	</select>
	
	<select id="cafelist" resultType="PosDto">
		select * from cafe;
	</select>
	
	<select id="pharlist" resultType="PosDto">
		select * from phar;
	</select>
	
	<select id="buslist" resultType="PosDto">
		select * from bus;
	</select>
	
	<select id="trainlist" resultType="PosDto">
		select * from train;
	</select>
</mapper>
