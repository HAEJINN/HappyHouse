<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.happyhouse.model.mapper.PosMapper">
	
	<select id="convenlist" resultType="PosDto">
		select * from conven;
	</select>
	
	<select id="cctvlist" resultType="PosDto">
		select * from cctv;
	</select>
</mapper>
